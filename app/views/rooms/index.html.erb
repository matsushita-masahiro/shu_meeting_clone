<h1>チャット一覧</h1>
<hr>
<% @rooms.each do |e| %>
  <a href="/rooms/<%= e.room_id %>"><%= e.room.name %></a>
  
<% end %>
<p><a href="/users/<%= current_user.id %>" class="btn btn-primary"><%= current_user.last_name %> <%= current_user.first_name %>さんの情報に戻る</a></p>
<p><a href="/" class="btn btn-info">トップに戻る</a></p>